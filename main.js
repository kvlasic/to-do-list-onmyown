const lists = document.querySelector('#lists')
const listInput = document.querySelector('#listInput')

let lists = [];
let todos = [];

//add a new list object to lists array
function addList() {
    let newList = { name: listsInput.value }
}

// render the list
function render() {
    lists.innerHTML = ""
}